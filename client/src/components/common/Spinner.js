import React from 'react';
import './Spinner.css'; // We will create this CSS file next

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
